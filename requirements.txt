# Runtime deps for the Python engine and the Vercel serverless functions.
# Deliberately tiny and pure-pip: `typst` ships prebuilt Rust wheels, so there is
# no external `typst` binary and no `pdftotext`/poppler dependency anywhere.
anthropic>=0.69,<1.0
typst>=0.13,<1.0
pypdf>=5.1,<7.0
pydantic>=2.9,<3.0
